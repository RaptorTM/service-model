import groovy.io.FileType
import hudson.model.*
import java.io.File;
import jenkins.model.Jenkins;

pipeline {
    agent any

    stages {
        stage('Load Classes') {
            steps {
                script {

                    def jenkinsRootDir = build.getEnvVars()["JENKINS_HOME"];
                    def parent = getClass().getClassLoader()
                    def loader = new GroovyClassLoader(parent)

//                     def someParameterValue = "abc"
                    println jenkinsRootDir
                    def groovyClass = loader.parseClass(readFile (jenkinsRootDir + "src\\modules\\config\\serviceModel.groovy")).newInstance()

                    def Services = groovyClass.getConfig()
                    println Services['dockerfile']
                }
            }
        }
//         stage('Run main.groovy') {
//             steps {
//                 // Запускаем файл main.groovy
//                 script {
//                     def groovyScript = load 'src\\Main.groovy'
//                     groovyScript.run_test_script()
//
//                 }
//             }
//         }
    }

    post {
        success {
            echo 'Pipeline succeeded!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}